import { Observable } from 'rxjs';
export declare class TuiDocCopyComponent {
    readonly texts$: Observable<[string, string]>;
    private readonly copy$;
    constructor(texts$: Observable<[string, string]>);
    get copied$(): Observable<boolean>;
    onClick(): void;
}
