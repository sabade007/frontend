import { AfterViewChecked, ComponentFactoryResolver, ComponentRef, ElementRef, Injector, OnDestroy } from '@angular/core';
import { TuiActiveZoneDirective, TuiNativeFocusableElement, TuiPortalService } from '@taiga-ui/cdk';
import { TuiDropdownBoxComponent } from '@taiga-ui/core/components/dropdown-box';
import { TuiDropdown } from '@taiga-ui/core/interfaces';
import { TuiDropdownWidthT, TuiHorizontalDirection, TuiVerticalDirection } from '@taiga-ui/core/types';
import { PolymorpheusContent } from '@tinkoff/ng-polymorpheus';
import { Observable } from 'rxjs';
export declare abstract class AbstractTuiDropdown implements TuiDropdown, AfterViewChecked, OnDestroy {
    private readonly componentFactoryResolver;
    private readonly injector;
    private readonly portalService;
    protected readonly elementRef: ElementRef<HTMLElement>;
    readonly activeZone: TuiActiveZoneDirective | null;
    abstract refresh$: Observable<unknown>;
    content: PolymorpheusContent;
    tuiDropdownHost: HTMLElement | TuiNativeFocusableElement | null;
    minHeight: number;
    maxHeight: number;
    align: TuiHorizontalDirection;
    direction: TuiVerticalDirection | null;
    sided: boolean;
    limitMinWidth: TuiDropdownWidthT;
    dropdownBoxRef: ComponentRef<TuiDropdownBoxComponent> | null;
    protected constructor(componentFactoryResolver: ComponentFactoryResolver, injector: Injector, portalService: TuiPortalService, elementRef: ElementRef<HTMLElement>, activeZone: TuiActiveZoneDirective | null);
    ngOnDestroy(): void;
    ngAfterViewChecked(): void;
    get clientRect(): ClientRect;
    get host(): HTMLElement | TuiNativeFocusableElement;
    get fixed(): boolean;
    protected toggleDropdown(value: boolean): void;
    protected openDropdownBox(): void;
    protected closeDropdownBox(): void;
}
