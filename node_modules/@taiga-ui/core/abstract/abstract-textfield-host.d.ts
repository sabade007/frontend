import { AbstractTuiControl } from '@taiga-ui/cdk';
import { TuiTextfieldHost } from '@taiga-ui/core/interfaces';
export declare abstract class TuiAbstractTextfieldHost<T extends AbstractTuiControl<any>> implements TuiTextfieldHost {
    protected readonly host: T;
    constructor(host: T);
    get readOnly(): boolean;
    get disabled(): boolean;
    get invalid(): boolean;
    get focusable(): boolean;
    get value(): string;
    abstract onValueChange(value: string): void;
    process(_input: HTMLInputElement): void;
}
