import { ElementRef, OnDestroy } from '@angular/core';
import { TuiActiveZoneDirective } from '@taiga-ui/cdk';
import { TuiHintService } from '@taiga-ui/core/services';
import { PolymorpheusContent } from '@tinkoff/ng-polymorpheus';
import { TuiAbstractHintOptions } from './abstract-hint-options';
export declare abstract class AbstractTuiHint implements OnDestroy {
    protected readonly elementRef: ElementRef<HTMLElement>;
    protected readonly hintService: TuiHintService;
    readonly activeZone: TuiActiveZoneDirective | null;
    protected readonly options: TuiAbstractHintOptions;
    mode: TuiAbstractHintOptions['mode'];
    direction: TuiAbstractHintOptions['direction'];
    content: PolymorpheusContent;
    protected constructor(elementRef: ElementRef<HTMLElement>, hintService: TuiHintService, activeZone: TuiActiveZoneDirective | null, options: TuiAbstractHintOptions);
    abstract getElementClientRect(): ClientRect;
    ngOnDestroy(): void;
    protected showTooltip(): void;
    protected hideTooltip(): void;
}
