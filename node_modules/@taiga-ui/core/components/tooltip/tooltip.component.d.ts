import { TuiHintOptions } from '@taiga-ui/core/directives';
import { TuiBrightness, TuiHintModeT } from '@taiga-ui/core/types';
import { PolymorpheusContent } from '@tinkoff/ng-polymorpheus';
import { Observable } from 'rxjs';
export declare class TuiTooltipComponent {
    private readonly isMobile;
    private readonly options;
    private globalMode;
    content: PolymorpheusContent;
    mode: TuiHintOptions['mode'];
    direction: TuiHintOptions['direction'];
    showDelay: TuiHintOptions['tuiHintShowDelay'];
    hideDelay: TuiHintOptions['tuiHintHideDelay'];
    describeId: string;
    constructor(destroy$: Observable<unknown>, mode$: Observable<TuiBrightness | null>, isMobile: boolean, options: TuiHintOptions);
    get computedMode(): TuiHintModeT | null;
    stopOnMobile(event: MouseEvent): void;
    get icon(): PolymorpheusContent;
}
