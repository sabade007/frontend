import { DoCheck } from '@angular/core';
import { TuiTextfieldController } from '@taiga-ui/core/directives';
import { TuiPrimitiveTextfieldComponent } from '../primitive-textfield.component';
export declare class TuiValueDecorationComponent implements DoCheck {
    private readonly textfield;
    private readonly controller;
    private readonly pre?;
    private readonly prefix$;
    readonly pre$: import("rxjs").Observable<number>;
    constructor(textfield: TuiPrimitiveTextfieldComponent, controller: TuiTextfieldController);
    get isContextTable(): boolean;
    get value(): string;
    get filler(): string;
    get prefix(): string;
    get postfix(): string;
    ngDoCheck(): void;
    private get placeholder();
    private get exampleText();
    private get decorationsVisible();
    private get focused();
    private get computedPostfix();
}
