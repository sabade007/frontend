import { ElementRef } from '@angular/core';
import { PolymorpheusContent } from '@tinkoff/ng-polymorpheus';
import { TuiLoaderOptions } from './loader-options';
export declare class TuiLoaderComponent {
    private readonly documentRef;
    private readonly elementRef;
    private readonly isIos;
    private readonly options;
    size: "m" | "s" | "xs" | "l" | "xl" | "xxl";
    inheritColor: boolean;
    overlay: boolean;
    textContent: PolymorpheusContent | null;
    set showLoader(value: boolean);
    loading: boolean;
    readonly isApple: boolean;
    constructor(documentRef: Document, elementRef: ElementRef<HTMLElement>, isIos: boolean, options: TuiLoaderOptions);
    get hasOverlay(): boolean;
    get hasText(): boolean;
    get isHorizontal(): boolean;
    get focused(): boolean;
}
