import { AnimationOptions } from '@angular/animations';
import { ElementRef, NgZone } from '@angular/core';
import { TuiBrightness } from '@taiga-ui/core/types';
import { Observable } from 'rxjs';
export declare class TuiScrollControlsComponent {
    private readonly options;
    private readonly ngZone;
    private readonly scrollRef;
    private readonly animationFrame$;
    readonly mode$: Observable<TuiBrightness | null>;
    readonly refresh$: Observable<[boolean, boolean] | boolean[]>;
    readonly animation: {
        readonly delay?: string | number | undefined;
        readonly params?: {
            [name: string]: any;
        } | undefined;
        readonly value: "";
    };
    constructor(options: AnimationOptions, ngZone: NgZone, scrollRef: ElementRef<HTMLElement>, animationFrame$: Observable<number>, mode$: Observable<TuiBrightness | null>);
    private get scrollbars();
}
