import { ElementRef, NgZone, Renderer2 } from '@angular/core';
import { TuiOrientationT } from '@taiga-ui/core/types';
import { Observable } from 'rxjs';
export declare class TuiScrollbarDirective {
    private readonly wrapper;
    private readonly container;
    private readonly doc;
    private readonly el;
    tuiScrollbar: TuiOrientationT;
    constructor(ngZone: NgZone, renderer: Renderer2, destroy$: Observable<void>, animationFrame$: Observable<number>, wrapper: ElementRef<HTMLElement>, container: ElementRef<HTMLElement>, doc: Document, el: ElementRef<HTMLElement>);
    private get scrolled();
    private get compensation();
    private get thumb();
    private get view();
    private get element();
    private getScrolled;
}
