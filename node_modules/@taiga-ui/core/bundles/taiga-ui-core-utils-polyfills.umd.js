(function (global, factory) {
    typeof exports === 'object' && typeof module !== 'undefined' ? factory(exports, require('@taiga-ui/cdk')) :
    typeof define === 'function' && define.amd ? define('@taiga-ui/core/utils/polyfills', ['exports', '@taiga-ui/cdk'], factory) :
    (global = global || self, factory((global['taiga-ui'] = global['taiga-ui'] || {}, global['taiga-ui'].core = global['taiga-ui'].core || {}, global['taiga-ui'].core.utils = global['taiga-ui'].core.utils || {}, global['taiga-ui'].core.utils.polyfills = {}), global.cdk));
}(this, (function (exports, cdk) { 'use strict';

    /**
     * @deprecated
     * TODO: remove in v3.0
     */
    // eslint-disable-next-line @typescript-eslint/naming-convention
    function innerHTML(documentRef) {
        var svg = documentRef.createElementNS("http://www.w3.org/2000/svg", "svg");
        if (svg.innerHTML !== undefined) {
            return;
        }
        var serializer = new XMLSerializer();
        var parser = new DOMParser();
        Object.defineProperty(SVGElement.prototype, "innerHTML", {
            get: function () {
                var result = [];
                var childNode = this.firstChild;
                while (childNode) {
                    result.push(serializer.serializeToString(childNode));
                    childNode = childNode.nextSibling;
                }
                return result.join("");
            },
            set: function (text) {
                while (this.firstChild) {
                    this.removeChild(this.firstChild);
                }
                try {
                    var svgDocElement = parser.parseFromString(text, "image/svg+xml").documentElement;
                    if (this.ownerDocument) {
                        this.appendChild(this.ownerDocument.importNode(svgDocElement, true));
                    }
                }
                catch (e) {
                    throw new cdk.TuiXmlParsingException();
                }
            },
        });
    }

    exports.innerHTML = innerHTML;

    Object.defineProperty(exports, '__esModule', { value: true });

})));
//# sourceMappingURL=taiga-ui-core-utils-polyfills.umd.js.map
