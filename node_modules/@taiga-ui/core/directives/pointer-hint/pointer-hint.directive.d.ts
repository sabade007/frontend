import { ElementRef } from '@angular/core';
import { TuiDestroyService, TuiHoveredService } from '@taiga-ui/cdk';
import { AbstractTuiHint } from '@taiga-ui/core/abstract';
import { TuiHintService } from '@taiga-ui/core/services';
import { PolymorpheusContent } from '@tinkoff/ng-polymorpheus';
import { TuiPointerHintOptions } from './pointer-hint-options';
export declare class TuiPointerHintDirective extends AbstractTuiHint {
    private readonly destroy$;
    protected readonly options: TuiPointerHintOptions;
    private currentMouseRect;
    tuiHintShowDelay: TuiPointerHintOptions['tuiHintShowDelay'];
    tuiHintHideDelay: TuiPointerHintOptions['tuiHintHideDelay'];
    set tuiPointerHint(value: PolymorpheusContent | null);
    content: PolymorpheusContent;
    constructor(elementRef: ElementRef<HTMLElement>, hintService: TuiHintService, destroy$: TuiDestroyService, hoveredService: TuiHoveredService, options: TuiPointerHintOptions);
    getElementClientRect(): ClientRect;
    private initMouseMoveSubscription;
    private mousePositionToClientRect;
}
