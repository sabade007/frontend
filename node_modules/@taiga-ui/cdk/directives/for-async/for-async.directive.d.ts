import { OnChanges, OnDestroy, TemplateRef, ViewContainerRef } from '@angular/core';
export declare class TuiForAsyncDirective<T> implements OnChanges, OnDestroy {
    private readonly view;
    private readonly template;
    private readonly destroy$;
    tuiForAsyncOf: readonly T[] | null | undefined;
    tuiForAsyncTimeout: number;
    constructor(view: ViewContainerRef, template: TemplateRef<unknown>);
    ngOnChanges(): void;
    ngOnDestroy(): void;
    private createAsyncViewForNewNodes;
    private clearViewForOldNodes;
}
