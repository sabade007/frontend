import { OnDestroy, TemplateRef } from '@angular/core';
import { TuiPortalService } from '@taiga-ui/cdk/components/portal-host';
export declare class TuiPortalDirective implements OnDestroy {
    private readonly templateRef;
    private readonly portalService;
    private viewRef?;
    set tuiPortal(show: boolean);
    constructor(templateRef: TemplateRef<Record<string, unknown>>, portalService: TuiPortalService);
    ngOnDestroy(): void;
}
