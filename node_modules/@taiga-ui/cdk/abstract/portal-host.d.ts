import { ComponentFactory, ComponentRef, ElementRef, EmbeddedViewRef, Injector, TemplateRef, ViewContainerRef } from '@angular/core';
import { AbstractTuiPortalService } from './portal-service';
/**
 * Abstract class for host element for dynamically created portals.
 */
export declare abstract class AbstractTuiPortalHostComponent {
    readonly elementRef: ElementRef<HTMLElement>;
    viewContainerRef: ViewContainerRef;
    constructor(elementRef: ElementRef<HTMLElement>, portalService: AbstractTuiPortalService);
    get clientRect(): ClientRect;
    addComponentChild<C>(componentFactory: ComponentFactory<C>, injector: Injector): ComponentRef<C>;
    addTemplateChild<C>(templateRef: TemplateRef<C>, context?: C): EmbeddedViewRef<C>;
}
