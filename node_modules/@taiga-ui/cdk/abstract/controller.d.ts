import { OnChanges } from '@angular/core';
import { Subject } from 'rxjs';
/**
 * @deprecated
 * TODO: 3.0 replace with {@link AbstractTuiController}
 */
export declare abstract class TuiController implements OnChanges {
    readonly change$: Subject<void>;
    ngOnChanges(): void;
}
export declare abstract class AbstractTuiController extends TuiController {
}
