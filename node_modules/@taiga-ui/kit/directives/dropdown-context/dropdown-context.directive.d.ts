import { ComponentFactoryResolver, ElementRef, Injector } from '@angular/core';
import { TuiActiveZoneDirective, TuiPortalService } from '@taiga-ui/cdk';
import { AbstractTuiDropdown, TuiDropdown } from '@taiga-ui/core';
import { PolymorpheusContent } from '@tinkoff/ng-polymorpheus';
import { Observable } from 'rxjs';
export declare class TuiDropdownContextDirective extends AbstractTuiDropdown implements TuiDropdown {
    protected readonly elementRef: ElementRef;
    private readonly documentRef;
    readonly activeZone: TuiActiveZoneDirective;
    private lastClickedClientRect;
    content: PolymorpheusContent;
    readonly refresh$: Observable<never>;
    readonly context: {
        close: () => void;
    };
    constructor(elementRef: ElementRef, documentRef: Document, destroy$: Observable<unknown>, componentFactoryResolver: ComponentFactoryResolver, injector: Injector, portalService: TuiPortalService, activeZone: TuiActiveZoneDirective);
    get clientRect(): ClientRect;
    get fixed(): boolean;
    private get dropdownContent();
    onHostClick(): void;
    onContextMenu(x: number, y: number): void;
    onAnotherContextOpen(target: HTMLElement): void;
    onArrow(event: KeyboardEvent, down: boolean): void;
    onKeyDownEsc(event: KeyboardEvent): void;
    private openDropdown;
    private checkIsFocusableElement;
}
