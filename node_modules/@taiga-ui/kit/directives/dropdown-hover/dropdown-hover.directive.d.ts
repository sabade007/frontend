import { ElementRef } from '@angular/core';
import { TuiHostedDropdownComponent } from '@taiga-ui/core';
import { Observable } from 'rxjs';
export declare class TuiDropdownHoverDirective {
    private readonly dropdown;
    constructor(destroy$: Observable<void>, { nativeElement }: ElementRef<HTMLElement>, dropdown: TuiHostedDropdownComponent);
    onMouseEnter(): void;
}
