import { FactoryProvider } from '@angular/core';
import { TuiHandler, TuiMonth } from '@taiga-ui/cdk';
import { TuiMonthPipe } from '@taiga-ui/core';
import { Observable } from 'rxjs';
export declare const TUI_MONTH_FORMATTER_PROVIDER: FactoryProvider;
/**
 * Range.setStart/set-end, except it uses offset in characters only
 */
export declare function monthFormatterFactory(pipe: TuiMonthPipe): TuiHandler<TuiMonth | null, Observable<string>>;
