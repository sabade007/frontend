import { InjectionToken, ValueProvider } from '@angular/core';
import { TuiSizeL, TuiSizeS } from '@taiga-ui/core/types';
import { TuiStatusT } from '@taiga-ui/kit/types';
export interface TuiTagOptions {
    readonly size: TuiSizeL | TuiSizeS;
    readonly status: TuiStatusT;
    readonly autoColor: boolean;
}
/** Default values for the tag options. */
export declare const TUI_TAG_DEFAULT_OPTIONS: TuiTagOptions;
export declare const TUI_TAG_OPTIONS: InjectionToken<TuiTagOptions>;
export declare const tuiTagOptionsProvider: (options: Partial<TuiTagOptions>) => ValueProvider;
