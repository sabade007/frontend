import { ElementRef, OnDestroy } from '@angular/core';
import { RouterLinkActive } from '@angular/router';
import { TuiFocusVisibleService } from '@taiga-ui/cdk';
import { TuiBrightness } from '@taiga-ui/core';
import { Observable } from 'rxjs';
export declare class TuiTabComponent implements OnDestroy {
    private readonly routerLinkActive;
    private readonly elementRef;
    readonly mode$: Observable<TuiBrightness | null>;
    readonly event$: Observable<Event>;
    readonly margin: number;
    readonly isIos: boolean;
    readonly isAndroid: boolean;
    focusVisible: boolean;
    constructor(routerLinkActive: RouterLinkActive | null, elementRef: ElementRef<HTMLElement>, mode$: Observable<TuiBrightness | null>, event$: Observable<Event>, margin: number, mobileAware: boolean, isIos: boolean, isAndroid: boolean, focusVisible$: TuiFocusVisibleService);
    get isActive(): boolean;
    ngOnDestroy(): void;
}
