import { FormArrayName, FormGroupDirective, FormGroupName, NgControl } from '@angular/forms';
import { TuiValidationError } from '@taiga-ui/cdk';
import { PolymorpheusContent } from '@tinkoff/ng-polymorpheus';
import { Observable } from 'rxjs';
export declare class TuiFieldErrorComponent {
    private readonly ngControl;
    private readonly formArrayName;
    private readonly formGroupName;
    private readonly formGroup;
    private readonly validationErrors;
    order: readonly string[];
    constructor(ngControl: NgControl | null, formArrayName: FormArrayName | null, formGroupName: FormGroupName | null, formGroup: FormGroupDirective | null, validationErrors: Record<string, Observable<PolymorpheusContent> | PolymorpheusContent>);
    get change$(): Observable<unknown>;
    get computedError(): Observable<TuiValidationError | null>;
    registerOnChange(): void;
    registerOnTouched(): void;
    setDisabledState(): void;
    writeValue(): void;
    private get error();
    private get invalid();
    private get touched();
    private get control();
    private get errorId();
    private get controlErrors();
    private getErrorId;
    private getError;
}
