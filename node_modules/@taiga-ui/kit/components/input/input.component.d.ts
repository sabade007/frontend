import { ChangeDetectorRef } from '@angular/core';
import { NgControl } from '@angular/forms';
import { AbstractTuiControl, TuiActiveZoneDirective, TuiContextWithImplicit, TuiFocusableElementAccessor } from '@taiga-ui/cdk';
import { TuiDataListHost, TuiHorizontalDirection } from '@taiga-ui/core';
import { PolymorpheusContent } from '@tinkoff/ng-polymorpheus';
export declare class TuiInputComponent extends AbstractTuiControl<string> implements TuiFocusableElementAccessor, TuiDataListHost<string> {
    private readonly hostedDropdown?;
    private readonly textfield?;
    icon: string | null;
    iconAlign: TuiHorizontalDirection;
    readonly datalist: PolymorpheusContent<TuiContextWithImplicit<TuiActiveZoneDirective>>;
    open: boolean;
    constructor(control: NgControl | null, changeDetectorRef: ChangeDetectorRef);
    get nativeFocusableElement(): HTMLInputElement | null;
    get focused(): boolean;
    get canOpen(): boolean;
    onValueChange(value: string): void;
    onHovered(hovered: boolean): void;
    onActiveZone(active: boolean): void;
    handleOption(item: unknown): void;
    protected getFallbackValue(): string;
    private focusInput;
    private setNativeValue;
}
