import { ChangeDetectorRef } from '@angular/core';
import { NgControl } from '@angular/forms';
import { AbstractTuiControl, TuiFocusableElementAccessor } from '@taiga-ui/cdk';
import { TuiBrightness, TuiHintControllerDirective, TuiSizeL, TuiSizeS, TuiTextfieldController } from '@taiga-ui/core';
import { Observable } from 'rxjs';
export declare const DEFAULT_ROWS = 20;
export declare const LINE_HEIGHT_M = 20;
export declare const LINE_HEIGHT_L = 24;
export declare class TuiTextAreaComponent extends AbstractTuiControl<string> implements TuiFocusableElementAccessor {
    readonly appearance: string;
    readonly isIOS: boolean;
    readonly mode$: Observable<TuiBrightness | null>;
    readonly controller: TuiTextfieldController;
    readonly hintController: TuiHintControllerDirective;
    private readonly focusableElement?;
    private readonly textfield?;
    rows: number;
    expandable: boolean;
    constructor(control: NgControl | null, changeDetectorRef: ChangeDetectorRef, appearance: string, isIOS: boolean, mode$: Observable<TuiBrightness | null>, controller: TuiTextfieldController, hintController: TuiHintControllerDirective);
    get labelOutside(): boolean;
    get nativeFocusableElement(): HTMLTextAreaElement | null;
    get focused(): boolean;
    get size(): TuiSizeL | TuiSizeS;
    get border(): number;
    get hasCleaner(): boolean;
    get hasTooltip(): boolean;
    get hasValue(): boolean;
    get hasCounter(): boolean;
    get hasPlaceholder(): boolean;
    get hasExampleText(): boolean;
    get computeMaxHeight(): number | null;
    get placeholderRaised(): boolean;
    get fittedContent(): string;
    get extraContent(): string;
    onFocused(focused: boolean): void;
    onHovered(hovered: boolean): void;
    onPressed(pressed: boolean): void;
    onValueChange(value: string): void;
    onMouseDown(event: MouseEvent): void;
    protected getFallbackValue(): string;
    private get lineHeight();
}
