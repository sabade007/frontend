import { TuiBrightness, TuiSizeL, TuiSizeXS } from '@taiga-ui/core';
import { TuiStatusT } from '@taiga-ui/kit/types';
import { PolymorpheusContent } from '@tinkoff/ng-polymorpheus';
import { Observable } from 'rxjs';
export declare class TuiBadgeComponent {
    readonly mode$: Observable<TuiBrightness | null>;
    value: PolymorpheusContent;
    size: TuiSizeL | TuiSizeXS;
    status: TuiStatusT;
    hoverable: boolean;
    constructor(mode$: Observable<TuiBrightness | null>);
    get padding(): string;
    get outputValue(): string;
    get isEmpty(): boolean;
}
