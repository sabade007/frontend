import { I18nPluralPipe } from '@angular/common';
import { ChangeDetectorRef } from '@angular/core';
import { NgControl } from '@angular/forms';
import { TuiContextWithImplicit, TuiFocusableElementAccessor, TuiNativeFocusableElement } from '@taiga-ui/cdk';
import { TuiDecimalT, TuiSizeL, TuiTextfieldController } from '@taiga-ui/core';
import { AbstractTuiInputSlider } from '@taiga-ui/kit/abstract';
import { TuiKeySteps } from '@taiga-ui/kit/types';
import { PolymorpheusContent } from '@tinkoff/ng-polymorpheus';
import { Observable } from 'rxjs';
/**
 * Turn on new `InputSlider`'s version.
 * The new version will behave almost the same as `InputSlider` from the next major release.
 * @deprecated TODO remove me in v3.0 and make `InputSlider` always "new".
 */
export declare class TuiNewInputSliderDirective {
}
export declare class TuiInputSliderComponent extends AbstractTuiInputSlider<number> implements TuiFocusableElementAccessor {
    readonly controller: TuiTextfieldController;
    readonly fromToTexts$: Observable<[string, string]>;
    readonly isNew: TuiNewInputSliderDirective | null;
    readonly i18nPlural: I18nPluralPipe;
    private readonly inputNumberRef?;
    private readonly sliderRef?;
    min: number;
    max: number;
    quantum: number;
    steps: number;
    segments: number;
    keySteps: TuiKeySteps | null;
    valueContent: PolymorpheusContent<TuiContextWithImplicit<number>>;
    prefix: string;
    postfix: string;
    /**
     * @deprecated use `tuiTextfieldCustomContent` instead
     * TODO delete in v3.0
     */
    secondary: string;
    constructor(control: NgControl | null, changeDetectorRef: ChangeDetectorRef, controller: TuiTextfieldController, fromToTexts$: Observable<[string, string]>, isNew: TuiNewInputSliderDirective | null, i18nPlural: I18nPluralPipe);
    get nativeFocusableElement(): TuiNativeFocusableElement | null;
    get focused(): boolean;
    get computedSteps(): number;
    get precision(): number;
    get decimal(): TuiDecimalT;
    get showValueContent(): boolean;
    /**
     * TODO remove old property `size` in v3.0
     */
    get computedSize(): TuiSizeL;
    /**
     * @deprecated for backward compatibility
     * TODO replace by just `this.valueContent` in v3.0
     */
    get computedValueContent(): PolymorpheusContent<TuiContextWithImplicit<number>>;
    focusTextInput(): void;
    safelyUpdateValue(value: number | null): void;
    onVerticalArrowKeyDown(coefficient: number): void;
    onSliderChange(newValue: number): void;
    onFocused(focused: boolean): void;
    onPressed(pressed: boolean): void;
    onHovered(hovered: boolean): void;
    private get textInputValue();
    protected getFallbackValue(): number;
    protected valueGuard(value: number): number;
    private updateTextInputValue;
}
