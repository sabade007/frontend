import { ElementRef, PipeTransform } from '@angular/core';
export declare abstract class AbstractTuiColorSegments<T> implements PipeTransform {
    private readonly userAgent;
    private readonly elementRef;
    private readonly isOldBrowsers;
    constructor(userAgent: string, elementRef: ElementRef<HTMLElement>);
    abstract transform(colors: string[]): T;
    protected calculate(colors: string[]): string;
}
