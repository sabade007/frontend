import { QueryList, TemplateRef } from '@angular/core';
import { TuiModeDirective, TuiSizeL } from '@taiga-ui/core';
import { Subject } from 'rxjs';
export declare class TuiBreadcrumbsWrapperComponent implements TuiModeDirective {
    size: TuiSizeL;
    readonly items: QueryList<TemplateRef<Record<string, unknown>>>;
    readonly change$: Subject<void>;
    readonly mode = "onLight";
    ngOnChanges(): void;
}
